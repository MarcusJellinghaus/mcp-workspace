# review-plan review log 2

## Round 1 — 2026-08-28
**Findings**:
I'll start by loading the tools I need and gathering context.I'll gather context in parallel: knowledge base files, plan files, and the issue.`pr_info/steps/step_5.md:96` — medium — `total_count = getattr(results, "totalCount", None)` is read unconditionally after the loop; on a zero-result search (and whenever `max_results` clamps to 0) no page was fetched, so PyGithub's `totalCount` property falls back to a `per_page=1` request — the exact extra search-API call this step's design exists to avoid — and the value is then discarded by `format_search_results`' empty-list early return. PyGithub also caches with `if not self.__totalCount`, so a genuine total of 0 re-requests on every call. Guard the read with `if items:`.

`pr_info/steps/step_3.md:1` — medium — step bundles a pure-message change (marker helper, both call sites, `## Other failed jobs` header) with a behaviour change (two new `truncate_ci_details` clamps plus threading `max_log_lines` through `branch_status_polling.py`) across two source and two test modules with seven test edits; splitting into 3a (threading + clamps, no message change) then 3b (marker unification + header) keeps every commit green and never leaves an intermediate commit violating "no notice names a parameter its caller does not accept".

`pr_info/steps/step_9.md:18` — medium — comment-only step with no test change and no behavioural change; planning_principles.md says "Merge tiny or intertwined steps", and this folds cleanly into step 8 (adjacent `file_tools` internal-cap comment, zero coupling). The stated justification — that the acceptance criterion covers three sites — does not dictate commit granularity.

`pr_info/steps/step_4.md:59` — low — no non-positive guard on `max_results`, although steps 3 and 5 explicitly clamp the same class of unvalidated tool parameter and cite each other for it; `github_issue_list(max_results=-1)` passes 0 to `manager.list_issues` (which returns one item, since `_list_issues_no_error_handling:253` breaks at `len >= 0`) and renders `... showing -1 of -1+ results` above an empty list.

`pr_info/steps/step_5.md:122` — low — GitHub's search API caps retrievable results at 1000, so when `totalCount` exceeds 1000 the notice's "raise max_results" advice cannot deliver the stated total; the plan does not consider this case. The "or refine your query" tail partially mitigates it.

`pr_info/steps/step_1.md:76` — low — the acceptance criterion names "the `_view` notice", but the plan asserts the two numbers and `max_lines` only against `truncate_output` directly and deliberately leaves `test_format_issue_view_truncation` / `test_format_pr_view_truncation` at `"truncated" in result`; one of them should assert the full notice so the criterion is covered at the tool-output level.

`pr_info/steps/step_3.md:31` — low — threading `max_lines=max_log_lines` into `format_for_llm` changes `check_branch_status` output and exceeds the issue's Decisions entry "Scope: Messages only, plus the `pr_feedback` reorder"; the justification is correct (I confirmed `format_report_for_llm` defaults `max_lines=300` and `async_poll_branch_status` never passes it, so `max_log_lines>300` is silently re-cut) and criterion 3 arguably requires it, but the scope expansion should be signed off rather than absorbed silently.
**Decisions**:
Verdict(decision='tasks', tasks=['In pr_info/steps/step_5.md around line 96, guard the `total_count = getattr(results, "totalCount", None)` read behind `if items:` so that zero-result searches (and any case where `max_results` clamps to 0) never trigger PyGithub\'s fallback `per_page=1` request; state this guard explicitly in the step\'s implementation notes and add a test asserting no extra search call is made for an empty result set.', 'Merge pr_info/steps/step_9.md into step 8: it is a comment-only step with no behavioural or test change, adjacent to the step 8 `file_tools` internal-cap comment, and planning_principles.md directs merging tiny steps. Move the three comment sites and the acceptance criterion into step 8 and delete step_9.md, renumbering any later steps and cross-references.', 'In pr_info/steps/step_4.md around line 59, add a non-positive guard/clamp on `max_results` for `github_issue_list`, matching the clamping already specified in steps 3 and 5, so that values <= 0 cannot yield the nonsensical `... showing -1 of -1+ results` header over an empty list; include a test for `max_results=-1` and `max_results=0`.', 'In pr_info/steps/step_1.md around line 76, strengthen one of `test_format_issue_view_truncation` / `test_format_pr_view_truncation` to assert the full `_view` truncation notice (both numbers and `max_lines`) rather than only `"truncated" in result`, so the acceptance criterion naming the `_view` notice is verified at the tool-output level and not just against `truncate_output` directly.'], escalate_reason=None)
**Changes**:
applied
