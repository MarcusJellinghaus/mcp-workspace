style: format verification module and split oversized test file

Apply Black formatting to src/mcp_workspace/git_operations/verification.py:
collapse multi-line expressions and string concatenations that fit on a
single line within the 88-character limit.

Split tests/git_operations/test_verification.py (1897 lines) into four
files, each well under the 750-line size threshold:

- test_verification.py: Tier 1 baseline + signing detection tests,
  retains the _patch_baseline_ok helper (~620 lines).
- test_verification_tier2_binary.py: Tier 2 binary checks
  (signing_binary, signing_key_accessible, gpg.program precedence)
  with the _patch_step5 helper (~520 lines).
- test_verification_tier2_aux.py: Tier 2 auxiliary checks
  (agent_reachable, allowed_signers, verify_head) with the _patch_step6
  helper (~346 lines).
- test_verification_tier3.py: Tier 3 actual_signature deep-probe tests
  and the end-to-end happy path with the _patch_step7 helper
  (~400 lines).

All 347 verification tests still pass; pylint, mypy, and the full test
suite (1290 passed, 1 skipped) remain green.
