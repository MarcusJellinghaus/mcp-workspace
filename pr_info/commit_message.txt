Apply black/isort formatting to permission probes and repo identifier files

Normalize whitespace and collapse short multi-line calls/asserts in three
recently added files that were committed without running the project's
formatters, restoring `black --check` and `isort --check` to a clean exit.

- src/mcp_workspace/github_operations/_permission_probes.py: collapse a
  short return-call onto one line.
- tests/github_operations/test_permission_probes.py: collapse five short
  multi-line patterns (two GithubException assignments, two assert
  parentheses, one set-comprehension membership check) onto single lines.
- tests/utils/test_repo_identifier.py: strip trailing whitespace after a
  class docstring.

No logic changes.
