Add Returns section to _format_deleted_paths docstring

Expand the one-line docstring of _format_deleted_paths into a
multi-section form with Args and Returns sections to satisfy ruff's
docstring-missing-returns (DOC201) rule, which failed the
ruff-docstrings CI step.

The Returns section documents that the function returns the original
path list unchanged when 20 or fewer entries exist, otherwise the
first 20 paths plus a summary line as entry #21. No behavioral change.
